{
    "tasks": [
        {
            "type": "shell",
            "label": "g++ build active file",
            "command": "pwd;cd build;make -j 8;cd -",
            "args": [],
            "options": {
                "cwd": "${workspaceFolder}"
            }
        }
    ],
    "version": "2.0.0"
}